const AppButton = ({ label }) => {
  return (
    <button 
      className="btn app-button"
      // onClick={onClick}
      // onDoubleClick={onDoubleClick}
    >
      {label}
    </button>
  )
}

export default AppButton